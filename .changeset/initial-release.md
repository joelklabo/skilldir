---
"skilldir": minor
---

Initial public release of `skilldir`.

Includes:

- first-source-wins skill discovery and resolution
- managed symlink reconciliation into a normal output directory
- `sync`, `status`, `doctor`, and `watch`
- CI, Changesets, and Pages scaffolding
